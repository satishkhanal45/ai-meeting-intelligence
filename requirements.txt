# AI Meeting Intelligence Platform
#
# pyproject.toml is the source of truth. Regenerate this file with:
#     uv export --no-hashes --no-dev --output-file requirements.txt
# It is kept only for `pip install -r requirements.txt` users.

pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1
networkx>=3.3
google-genai>=1.0.0
groq>=0.9.0
httpx>=0.27.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
